# config file version
apiVersion: 1

# list of datasources to insert/update depending
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    url: http://prometheus:9090
    version: 1
    editable: false